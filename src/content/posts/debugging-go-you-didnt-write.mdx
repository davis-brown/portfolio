---
title: "Debugging Go services you didn't write"
summary: "Production defects, Splunk traces, and the slow work of building a mental model of a service under load."
date: 2026-03-17
minutes: 5
draft: false
---

[ PLACEHOLDER — the real post goes here. The title and summary above are a
starting point drawn from work you have actually shipped; rewrite or delete. ]

## A heading

Body copy renders with the site's type tokens: 18px, 1.7 line height, capped at
about 68ch so a paragraph stays readable.

```ts
// Code blocks scroll horizontally rather than widening the page.
const resolvers = {
  Query: {
    employee: (_: unknown, { id }: { id: string }, ctx: Context) =>
      ctx.loaders.employee.load(id),
  },
};
```

Another paragraph, so the spacing between blocks stays visible while the design
is still being checked.
